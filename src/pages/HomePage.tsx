import logo from '../assets/logo.png';

const HomePage = () => {
  return (
    <div className='section'>
      <img src={logo} alt='' />
      <div>HomePage</div>
    </div>
  );
};

export default HomePage;
